services:
  stream:
    image: "replace_me"
    user: "${CONTAINER_UID}:${CONTAINER_GID}"
    command: ["-m", "millegrilles_streaming"]
    x-millegrilles-certificat:
      roles:
        - stream
      exchanges:
        - 2.prive
        - 1.public
      dns:
        localhost: true
        hostnames:
          - stream
        domain: true
    environment:
      - MQ_HOSTNAME=${MQ_HOSTNAME:-mq}
      - MQ_PORT=${MQ_PORT:-5673}
      - MG_REDIS_HOST=redis
      - MG_REDIS_PORT=6379
      - REDIS_PASSWORD_PATH=/run/secrets/passwd.redis.txt
      - KEY_PATH=/run/secrets/key_cert.pem
      - CA_PATH=/run/secrets/millegrille.pem
      - DIR_STAGING=/var/opt/millegrilles/staging/streaming
    volumes:
      - ${MILLEGRILLES_ROOT}/var/staging:/var/opt/millegrilles/staging
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 100M
      restart_policy:
        condition: on-failure
        delay: 20s
        max_attempts: 1000
    configs:
      - source: mgca
        target: /run/secrets/millegrille.pem
    secrets:
      - source: stream
        target: key_cert.pem
      - source: passwd_redis
        target: passwd.redis.txt
    hostname: stream
    networks:
      - app_net

networks:
  app_net:
    name: ${INSTANCE_NAME}_net
    external: true

configs:
  mgca:
    file: ${MILLEGRILLES_ROOT}/etc/millegrille.pem

secrets:
  stream:
    file: ${MILLEGRILLES_ROOT}/secrets/stream.pem
  passwd_redis:
    file: ${MILLEGRILLES_ROOT}/secrets/redis.txt
